name: Build Linux

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Tag to build (e.g. v1.0.0)'
        required: false
        type: string

permissions:
  contents: read

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_tag || github.ref }}
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install Tesseract and Qt runtime libraries
        run: |
          sudo apt-get update
          # libegl1/libgl1 & friends are not preinstalled on ubuntu-latest;
          # PySide6 fails to import without them (libEGL.so.1 missing).
          sudo apt-get install -y tesseract-ocr libegl1 libgl1 libxkbcommon0 libdbus-1-3 libfontconfig1

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        # Headless runner: no X display, so Qt must use the offscreen platform.
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -m pytest tests/ -v

      - name: Build executable
        run: python build.py

      # build.py now produces an onedir bundle (dist/QuickPdfOcr/QuickPdfOcr
      # plus dist/QuickPdfOcr/_internal/), not a single dist/QuickPdfOcr
      # executable -- a onedir binary cannot run without its _internal/
      # sibling, so archive the whole directory as one runnable unit.
      - name: Create tarball
        run: tar -czf dist/QuickPdfOcr-Linux-x86_64.tar.gz -C dist QuickPdfOcr

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: QuickPdfOcr-Linux-x86_64
          path: dist/QuickPdfOcr-Linux-x86_64.tar.gz
          if-no-files-found: error
